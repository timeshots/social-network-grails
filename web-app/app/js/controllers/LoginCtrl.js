"use strict";

angular.module('LoginCtrl', ['UserService'])
    .controller('LoginCtrl', ['$scope', '$location', '$http', 'UserService', 'SessionService', '$timeout',
        function($scope, $location, $http, $timeout, User, Session){

        }
    ]);